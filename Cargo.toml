[package]
name = "portable-mutex"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[features]
default = []

[build-dependencies]
bindgen = "0.72.1"

[dependencies]
windows-sys = { version = "0.61.2", features = ["Win32_System_Threading"] }
